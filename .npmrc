# Auto-approve build scripts for known-safe dependencies.
# sharp: Next.js image optimization (required).
# unrs-resolver: ESLint rule resolver (required for next lint).
auto-install-peers=true
strict-peer-dependencies=false
